{
  "name": "prinsight",
  "version": "1.0.0",
  "description": "AI-powered GitHub PR review bot — auto-reviews pull requests using Gemini API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "dev:client": "cd client && npm run dev",
    "build:client": "cd client && npm install && npm run build",
    "setup": "npm install && cd client && npm install"
  },
  "keywords": ["github", "pull-request", "code-review", "gemini", "ai", "webhook"],
  "author": "Akash Sharma",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@octokit/rest": "^20.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
